"use client";

import Link from "next/link";
import { usePathname, useRouter } from "next/navigation";
import { useState } from "react";
import { BarChart3, CalendarDays, CircleHelp, CreditCard, LayoutDashboard, LogOut, Menu, Settings, Target, TrendingUp, Wallet, X } from "@/components/icons";
import { Button } from "@/components/ui/button";
import { SpaceSwitcher } from "@/components/dashboard/space-switcher";
import { LogoutConfirmationDialog } from "@/components/dashboard/logout-confirmation-dialog";
import type { AvailableSpace } from "@/server/space";
import { cn } from "@/lib/utils";

const links = [{ label: "Visão geral", href: "/", icon: LayoutDashboard }, { label: "Transações", href: "/transactions", icon: Wallet }, { label: "Contas", href: "/accounts", icon: CreditCard }, { label: "Planejamento", href: "/planning", icon: Target }, { label: "Investimentos", href: "/investments", icon: TrendingUp }, { label: "Relatórios", href: "/reports", icon: BarChart3 }];

export function SidebarClient({ spaces }: { spaces: AvailableSpace[] }) {
  const pathname = usePathname(); const router = useRouter(); const [open, setOpen] = useState(false); const [logoutOpen, setLogoutOpen] = useState(false); const [pending, setPending] = useState(false);
  async function logout() { setPending(true); try { const response = await fetch("/api/auth/sign-out", { method: "POST", headers: { "content-type": "application/json" }, body: "{}" }); if (response.ok) { router.push("/login"); router.refresh(); } } finally { setPending(false); } }
  return <><aside className={cn("fixed inset-y-0 left-0 z-40 flex h-dvh max-h-dvh w-64 -translate-x-full flex-col overflow-y-auto bg-sidebar p-5 text-sidebar-foreground [scrollbar-width:none] [&::-webkit-scrollbar]:hidden transition-transform lg:translate-x-0", open && "translate-x-0")}><div className="flex items-center justify-between"><Link href="/" className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-xl bg-primary font-black text-primary-foreground">F</span><span className="font-display text-xl font-semibold tracking-tight text-sidebar-foreground">finansee<span className="text-primary">.</span></span></Link><Button variant="ghost" size="icon" className="text-sidebar-foreground/80 hover:!bg-sidebar-accent hover:!text-sidebar-foreground lg:hidden" onClick={() => setOpen(false)} aria-label="Fechar menu"><X /></Button></div><div className="mt-9"><SpaceSwitcher spaces={spaces} /></div><nav className="mt-8 flex flex-1 flex-col gap-1"><p className="mb-2 px-3 text-[10px] font-bold uppercase tracking-[0.2em] text-sidebar-foreground/55">Menu principal</p>{links.map(({ label, href, icon: Icon }) => <Link key={label} href={href} onClick={() => setOpen(false)} className={cn("flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-medium transition-colors", pathname === href ? "bg-sidebar-accent text-sidebar-foreground" : "text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-sidebar-foreground")}><Icon className="size-4" />{label}{label === "Planejamento" && <span className="ml-auto rounded-full bg-primary/15 px-2 py-0.5 text-[10px] text-primary">novo</span>}</Link>)}</nav><p className="mb-2 mt-8 px-3 text-[10px] font-bold uppercase tracking-[0.2em] text-sidebar-foreground/55">Preferências</p><Link href="/calendar" onClick={() => setOpen(false)} className={cn("flex items-center gap-3 rounded-xl px-3 py-3 text-sm", pathname === "/calendar" ? "bg-sidebar-accent text-sidebar-foreground" : "text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-sidebar-foreground")}><CalendarDays className="size-4" />Calendário</Link><Link href="/settings" onClick={() => setOpen(false)} className={cn("flex items-center gap-3 rounded-xl px-3 py-3 text-sm", pathname === "/settings" ? "bg-sidebar-accent text-sidebar-foreground" : "text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-sidebar-foreground")}><Settings className="size-4" />Configurações</Link><div className="mt-auto border-t border-sidebar-border pt-4"><Link href="/help" onClick={() => setOpen(false)} className={cn("flex items-center gap-3 rounded-xl px-3 py-3 text-sm", pathname === "/help" ? "bg-sidebar-accent text-sidebar-foreground" : "text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-sidebar-foreground")}><CircleHelp className="size-4" />Central de ajuda</Link><button type="button" onClick={() => setLogoutOpen(true)} className="flex min-h-11 w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm text-sidebar-foreground/70 transition-colors hover:bg-sidebar-accent hover:text-sidebar-foreground"><LogOut className="size-4" />Sair</button></div></aside><Button variant="outline" size="icon" className="mobile-menu-trigger fixed left-4 top-4 z-30 lg:hidden" onClick={() => setOpen(true)} aria-label="Abrir menu"><Menu /></Button><LogoutConfirmationDialog open={logoutOpen} pending={pending} onOpenChange={setLogoutOpen} onConfirm={logout} /></>;
}
