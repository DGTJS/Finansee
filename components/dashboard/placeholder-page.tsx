import { CalendarDays, Settings } from "@/components/icons";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export function PlaceholderPage({ kind }: { kind: "calendar" | "settings" }) {
  const calendar = kind === "calendar";
  return <main className="min-h-screen bg-background lg:pl-64"><div className="mx-auto max-w-[1200px] px-5 pb-12 pt-20 sm:px-8 lg:px-10 lg:pt-24"><header className="border-b border-border pb-7"><p className="text-sm text-muted-foreground">Preferências do espaço</p><h1 className="mt-2 font-display text-3xl font-semibold tracking-tight sm:text-4xl">{calendar ? "Calendário" : "Configurações"}</h1><p className="mt-2 text-sm text-muted-foreground">{calendar ? "Veja vencimentos e competências em uma visão mensal." : "Ajuste o comportamento do seu espaço financeiro."}</p></header><Card className="mt-6 max-w-2xl"><CardHeader><CardTitle className="flex items-center gap-2">{calendar ? <CalendarDays /> : <Settings />}{calendar ? "Calendário financeiro" : "Preferências gerais"}</CardTitle><CardDescription>Esta área já está preparada para receber os próximos controles do painel.</CardDescription></CardHeader><CardContent><div className="grid min-h-44 place-items-center rounded-2xl border border-dashed border-border text-center"><p className="max-w-sm text-sm text-muted-foreground">{calendar ? "A integração de vencimentos será adicionada sobre os dados do banco local." : "Idioma, aparência e preferências de notificações serão configuráveis aqui."}</p></div></CardContent></Card></div></main>;
}
